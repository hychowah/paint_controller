"""Presentation and shell models for QML (TD-055 honesty).

This package is **not** a domain-model layer. Most modules are QML façades
(*Status read projections, *Actions command boundaries, shell policy).
New domain/policy rules belong under ``handlers/policy`` (plain Python) or
application handlers — not here.
"""

__all__ = []
