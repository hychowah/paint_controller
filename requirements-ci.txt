# CI-only Python deps for typecheck/test jobs (TD-045).
# Omits PyGObject and vtk: tests stub gi when missing; vtk is widgets/scripts only.
# Device installs still use python/paint_controller/requirements.txt (full stack).

# Qt6 UI Framework
PySide6~=6.5

# Scientific
numpy~=1.24
opencv-python-headless>=4.10,<5

# Data serialization
PyYAML~=6.0

# SSH for remote connections
paramiko~=3.0

# Steam Deck HID (tests also stub hid; wheel usually available)
hidapi~=0.14

# Dev / gate tools
-r requirements-dev.txt
