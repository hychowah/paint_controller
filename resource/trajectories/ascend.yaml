name: "ascend"
description: "ascend"

actions:
  - id: "Ascend_to_top"
    name: "winch_move"
    type: "winch_absolute"
    params:
      length: 7800  # mm (target position)
      speed: 200    # mm/s
    wait_for_completion: true
    # Duration auto-calculated: 1000mm / 100mm/s = 10 seconds
