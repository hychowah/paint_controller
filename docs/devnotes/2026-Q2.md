# DEVNOTES Archive: 2026 Q2 (April – June)

> Archived 2026-07-23. Entries rotated from DEVNOTES.md per the 90-day policy in AGENTS.md.

---

### 2026-04-28 00:00 - Large File Cleanup Wave Guard Rails And PageHome Retirement

**Goal**: Reduce oversized test and QML files without widening the current architecture program, starting with reusable guard/test support and the last named `PageHome.qml` preview remainder.
**Issues**: The repo had several justified large files, but the highest-value cleanup targets still mixed too many concerns in one place. The local failures during the slice were both harness defects rather than production regressions: extracted startup-smoke helpers left a few missing imports behind, and a new direct `PageHome.qml` harness needed escaped braces around an inline QML object literal inside a Python f-string.
**Tried**: Split startup-smoke support into `tests/startup_smoke_support.py`, broke shell/navigation and PageHome startup coverage into focused files, split controller-factory/AppRuntime recorder helpers into `tests/controller_factory_runtime_support.py`, added direct `AppRuntime` and `EditWorkFlowTab` smoke coverage, extracted `HomeVideoPanel.qml`, moved `PageHome.qml` onto explicit `videoRuntime` ownership, and extracted the workflow-editor load/save popups into standalone system-control components before rerunning the focused validation bands after each slice.
**Result**: ✅ The cleanup wave is moving with bounded slices instead of another framework rewrite. Startup-smoke and runtime guard files are smaller and more local, `PageHome.qml` no longer depends on the ambient base-stream preview path for the touched video surface, the new focused validation bands are green at `27 passed` for `tests/test_startup_smoke_shell.py` plus `tests/test_startup_smoke.py`, `7 passed` for `tests/test_app_runtime_runtime.py` plus `tests/test_controller_factory_runtime.py`, `10 passed` for `tests/test_startup_smoke_home.py` plus `tests/test_startup_smoke_shell.py` plus `tests/test_qml_imports.py`, and `2 passed` for `tests/test_startup_smoke_workflow_editor.py` plus `tests/test_qml_imports.py`, and `EditWorkFlowTab.qml` is smaller without changing the workflow editor contract.
**Files**: `tests/startup_smoke_support.py`, `tests/test_startup_smoke.py`, `tests/test_startup_smoke_shell.py`, `tests/test_startup_smoke_home.py`, `tests/test_startup_smoke_workflow_editor.py`, `tests/controller_factory_runtime_support.py`, `tests/test_app_runtime_runtime.py`, `tests/test_controller_factory_runtime.py`, `python/paint_controller/qml/pages/home/PageHome.qml`, `python/paint_controller/qml/pages/home/HomeVideoPanel.qml`, `python/paint_controller/qml/pages/home/qmldir`, `python/paint_controller/qml/core/MainWindow.qml`, `python/paint_controller/qml/overlays/systemcontrol/EditWorkFlowTab.qml`, `python/paint_controller/qml/overlays/systemcontrol/components/WorkflowSelectorPopup.qml`, `python/paint_controller/qml/overlays/systemcontrol/components/WorkflowSaveAsPopup.qml`, `python/paint_controller/qml/overlays/systemcontrol/components/qmldir`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/tech-debt.md`, `DEVNOTES.md`

---

### 2026-04-28 00:00 - Workstream E Fullscreen Overlay Telemetry Retirement

**Goal**: Retire the remaining fullscreen wheel and winch telemetry read paths without widening the slice into `PageHome.qml` preview cleanup, base-top-view architecture changes, or another generic video/device contract.
**Issues**: The remaining fullscreen video/runtime remainder was narrower than the family label implied: `BaseFrontOverlay.qml` still read wheel speed/current/travel directly from `wheelController`, and `EndEffectorOverlay.qml` still read winch torque and cable length directly from `winchController`, even though bounded `wheelStatus` and `winchStatus` already existed. The focused validation failures were both local harness defects: first an escaped-brace error in a new inline QML object literal, then an unrelated existing `VideoOverlayTopBar.qml` warning in the direct `BaseFrontOverlay.qml` harness that needed the assertions scoped to the actual wheel-retirement failure signals.
**Tried**: Added explicit `wheelStatus` and `winchStatus` required props to the touched fullscreen overlays, threaded those contracts through `VideoFullscreenWorkspace.qml`, `VideoFullscreenOverlay.qml`, `MainWindow.qml`, and `MultiScreenListUI.qml`, rewired the fullscreen wheel and winch text fields to the bounded models, tightened the fullscreen workspace and wrapper smoke harnesses to null raw `wheelController` and `winchController`, added a direct `BaseFrontOverlay.qml` smoke harness, updated the direct `EndEffectorOverlay.qml` harness to pass `winchStatus`, fixed the local harness defects, reran the focused startup/import band, then reran the full suite.
**Result**: ✅ The fullscreen overlay telemetry slice is landed. `BaseFrontOverlay.qml` and `EndEffectorOverlay.qml` now consume bounded `wheelStatus` and `winchStatus` instead of raw ambient controller reads for the touched telemetry, the focused validation band is green at `8 passed`, and the full suite is green at `260 passed`.
**Files**: `python/paint_controller/qml/overlays/video/components/BaseFrontOverlay.qml`, `python/paint_controller/qml/overlays/video/components/EndEffectorOverlay.qml`, `python/paint_controller/qml/features/video/VideoFullscreenWorkspace.qml`, `python/paint_controller/qml/overlays/video/VideoFullscreenOverlay.qml`, `python/paint_controller/qml/core/MainWindow.qml`, `python/paint_controller/qml/overlays/MultiScreenListUI.qml`, `tests/test_startup_smoke.py`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/tech-debt.md`, `INDEX.md`, `DEVNOTES.md`

---

### 2026-04-28 00:00 - Workstream E Lidar And Monitor Telemetry Retirement

**Goal**: Retire the remaining wall-detection and monitor lidar read paths without widening the slice into PageHome preview cleanup, fullscreen winch telemetry, or 3D lidar visualization redesign.
**Issues**: The remaining lidar seam was narrow but real: the touched fullscreen and monitor surfaces still depended on raw ambient `lidarController` reads, and both used `Connections`, so the replacement contract needed a real QObject signal instead of a passive property bag. The main local risk was caller-threading drift because the fullscreen path crosses `MainWindow.qml`, `MultiScreenListUI.qml`, `VideoFullscreenWorkspace.qml`, and `EndEffectorOverlay.qml` before it reaches `WallDetectionOverlay.qml`.
**Tried**: Added bounded `_LidarStatus` in `AppRuntime`, exposed it to QML as `lidarStatus`, threaded that contract through the main and secondary fullscreen callers plus `PageMonitor.qml`, rewired `WallDetectionOverlay.qml` and the monitor lidar chart/header to the bounded model, extended the runtime seam recorder/assertions, updated the startup-smoke context bundle with a QObject-shaped lidar status fake, tightened the multiscreen and fullscreen smoke harnesses to null `lidarController`, reran the focused runtime/startup/import band, then reran the full suite.
**Result**: ✅ The lidar/monitor telemetry slice is landed. `WallDetectionOverlay.qml` and the monitor lidar chart/header now consume bounded `lidarStatus` instead of raw ambient `lidarController` reads, the focused validation band is green at `8 passed`, and the full suite is green at `259 passed`.
**Files**: `python/paint_controller/core/app_runtime.py`, `python/paint_controller/qml/core/MainWindow.qml`, `python/paint_controller/qml/overlays/MultiScreenListUI.qml`, `python/paint_controller/qml/features/video/VideoFullscreenWorkspace.qml`, `python/paint_controller/qml/overlays/video/VideoFullscreenOverlay.qml`, `python/paint_controller/qml/overlays/video/components/EndEffectorOverlay.qml`, `python/paint_controller/qml/overlays/video/components/WallDetectionOverlay.qml`, `python/paint_controller/qml/pages/status/PageMonitor.qml`, `tests/test_startup_smoke.py`, `tests/test_controller_factory_runtime.py`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/tech-debt.md`, `INDEX.md`, `DEVNOTES.md`

---

### 2026-04-28 00:00 - Workstream E Teensy And End-Effector Detail Telemetry Retirement

**Goal**: Retire the remaining shared teensy/detail and end-effector valve read paths without widening the slice into fullscreen winch remainder or controller write-path redesign.
**Issues**: The next live family was not another simple QML rebinding pass because the existing `teensyStatus` contract did not expose IMU, arm-detail, or gimbal-angle fields, and there was no bounded valve telemetry contract at all. The only focused validation failures were local harness defects: inline QML object literals in new startup-smoke tests needed escaped braces, and a direct `EndEffectorOverlay.qml` harness needed the same local runtime-model shape as the fullscreen workspace instead of assuming the shell root context alone was enough.
**Tried**: Extended `_TeensyStatus` in `AppRuntime` with only the touched IMU, arm, and gimbal-angle fields, added a bounded `_ValveStatus`, threaded `valveStatus` through the main and secondary fullscreen/video callers plus `PageMonitor.qml`, rewired `IMUCard.qml`, `TeensyArmCard.qml`, `ValvesCard.qml`, and the touched teensy/valve reads in `EndEffectorOverlay.qml`, updated runtime seam assertions, extended the shared startup-smoke context bundle, added direct null-global smoke harnesses for the touched cards and overlay, fixed the local harness defects, and reran the focused runtime/startup/import band.
**Result**: ✅ The teensy/end-effector detail slice is landed. The shared IMU, arm, valve, and touched fullscreen end-effector telemetry surfaces now consume bounded `teensyStatus` and `valveStatus` inputs instead of raw ambient `teensyController.all_status` or `esp32ValveController` reads, fullscreen winch telemetry remains explicit remainder, and the focused validation band is green at `11 passed` across the touched runtime, startup-smoke, and QML-import checks.
**Files**: `python/paint_controller/core/app_runtime.py`, `python/paint_controller/qml/pages/status/components/IMUCard.qml`, `python/paint_controller/qml/pages/status/components/TeensyArmCard.qml`, `python/paint_controller/qml/pages/status/components/ValvesCard.qml`, `python/paint_controller/qml/pages/status/PageMonitor.qml`, `python/paint_controller/qml/features/video/VideoFullscreenWorkspace.qml`, `python/paint_controller/qml/overlays/video/components/EndEffectorOverlay.qml`, `python/paint_controller/qml/core/MainWindow.qml`, `python/paint_controller/qml/overlays/MultiScreenListUI.qml`, `python/paint_controller/qml/overlays/video/VideoFullscreenOverlay.qml`, `tests/test_startup_smoke.py`, `tests/test_controller_factory_runtime.py`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/tech-debt.md`, `INDEX.md`, `DEVNOTES.md`

---

### 2026-04-28 00:00 - Workstream E Winch Detail Telemetry Retirement

**Goal**: Retire the remaining page-level and shared-card winch detail read paths without widening the slice into winch command-path redesign or fullscreen telemetry remainder.
**Issues**: `PageWinch.qml` still mixed many raw `winchController` read-side telemetry and enabled-gate bindings with handler-owned write actions, and `WinchCard.qml` still read raw winch telemetry even though the existing bounded `winchStatus` contract already exposed the needed values. The first focused validation failure was local to the new shared-card harness choice: a direct `PageMonitor.qml` smoke check surfaced unrelated existing teensy/lidar remainder instead of isolating the touched winch path.
**Tried**: Rebound the remaining `PageWinch.qml` read-side enabled, warning, and telemetry bindings onto `winchStatus`, rewired `WinchCard.qml` to an explicit `winchStatus` input, threaded that contract through `PageMonitor.qml` and `MultiScreenListUI.qml`, tightened the `PageWinch.qml` startup smoke harness to null `winchController`, replaced the over-broad `PageMonitor.qml` smoke check with a direct `WinchCard.qml` harness, and reran the focused startup/import band.
**Result**: ✅ The page-level and shared-card winch detail seams are landed. `PageWinch.qml` and `WinchCard.qml` now consume `winchStatus` for the touched telemetry and read-side state, `MultiScreenListUI.qml` passes the shared card contract explicitly through `PageMonitor.qml`, and the focused validation band is green at `5 passed` for the targeted startup-smoke tests plus `tests/test_qml_imports.py`.
**Files**: `python/paint_controller/qml/pages/winch/PageWinch.qml`, `python/paint_controller/qml/pages/status/components/WinchCard.qml`, `python/paint_controller/qml/pages/status/PageMonitor.qml`, `python/paint_controller/qml/overlays/MultiScreenListUI.qml`, `tests/test_startup_smoke.py`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/tech-debt.md`, `INDEX.md`, `DEVNOTES.md`

---

### 2026-04-28 00:00 - Workstream E Wheel Detail Telemetry Retirement

**Goal**: Retire the remaining page-level wheel detail read path in `PageWheel.qml` without widening the slice into fullscreen wheel telemetry or base preview wiring.
**Issues**: `PageWheel.qml` still depended on raw `wheelController` for enabled state, wheel speed, current, and position even though the existing bounded `wheelStatus` contract already exposed those fields. The first focused startup smoke failure was local to the new harness: the initial check used a Python-side dynamic object shape that silently tolerated undefined fields for some bindings and still tripped `toFixed()` on position values.
**Tried**: Threaded `wheelStatus` explicitly into `PageWheel.qml` from `MainWindow.qml`, replaced the page's raw `wheelController` telemetry bindings with `wheelStatus`, added a direct `PageWheel.qml` startup smoke harness, then tightened the harness to pass an inline JS `wheelStatusModel` while forcing `wheelController` to `null` so any fallback raw read would fail visibly.
**Result**: ✅ The page-level wheel detail seam is landed. `PageWheel.qml` now consumes the bounded `wheelStatus` contract for the touched telemetry and enabled-state bindings, the preview path remains explicit `baseStreamHandler` remainder, and the focused validation band is green at `2 passed` for `tests/test_startup_smoke.py -k page_wheel_loads_with_explicit_wheel_status` plus `tests/test_qml_imports.py`.
**Files**: `python/paint_controller/qml/pages/wheel/PageWheel.qml`, `python/paint_controller/qml/core/MainWindow.qml`, `tests/test_startup_smoke.py`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/tech-debt.md`, `INDEX.md`, `DEVNOTES.md`

---

### 2026-04-28 00:00 - Architecture Planning Docs Full Rewrite

**Goal**: Replace the overlapping architecture planning documents with a simpler control plane that matches the repo’s real state.
**Issues**: The planning stack had drifted into duplicate live trackers, stale next-step language, and more framework than the remaining work needed, which increased the risk of future implementation sessions following the wrong source of truth.
**Tried**: Rewrote `docs/plan/00_ARCHITECTURE_PROGRESS.md` as the single live execution board, rewrote `docs/plan/01_PYTHON_QT_ARCHITECTURE_DEBT_PLAN.md` as durable architecture guidance plus historical context, and aligned `INDEX.md`, `docs/tech-debt.md`, and `PLANNING.md` to the new control plane.
**Result**: ✅ The planning docs now separate live execution from durable rationale, the next family order is explicit, frozen contracts and quarantined remainder are named directly, and future sessions have one live source of truth for unfinished architecture work.
**Files**: `PLANNING.md`, `docs/plan/00_ARCHITECTURE_PROGRESS.md`, `docs/plan/01_PYTHON_QT_ARCHITECTURE_DEBT_PLAN.md`, `docs/tech-debt.md`, `INDEX.md`, `DEVNOTES.md`
